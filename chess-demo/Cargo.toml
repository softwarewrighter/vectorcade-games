[package]
name = "chess-demo"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Vector chess board demo for VectorCade"

[dependencies]
vectorcade-shared = { version = "0.1.0" }
glam = { version = "0.27", default-features = false, features = ["libm"] }
