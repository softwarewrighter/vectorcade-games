[package]
name = "battlezone"
version = "0.1.0"
edition = "2024"
license = "MIT"
description = "Battlezone game for VectorCade"

[dependencies]
vectorcade-shared = { version = "0.1.0" }
glam = { version = "0.27", default-features = false, features = ["libm"] }
